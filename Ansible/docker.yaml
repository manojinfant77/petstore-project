- name: Build and Deploy Petstore
  hosts: local
  become: yes
  tasks:
    - name: Update apt package cache
      apt:
        update_cache: yes

    - name: Log in to Docker Hub
      community.docker.docker_login:
        registry_url: https://index.docker.io/v1/
        username: "manojinfant77"
        password: "dckr_pat_FcbnTXfPlpEheUD0w8t_3EgNfq8"

    - name: Build and Push Docker image
      community.docker.docker_image:
        name: "manojinfant77/petstore"
        tag: latest
        build:
          path: /var/lib/jenkins/workspace/petstore
        push: yes
        source: build

    - name: Run Petstore container
      community.docker.docker_container:
        name: pet1
        image: "manojinfant77/petstore:latest"
        state: started
        restart_policy: always
        published_ports:
          - "8081:8080"
